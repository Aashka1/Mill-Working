# Production dependencies actually imported by server.py.
# The original 130-package Emergent environment lock is kept in
# requirements-emergent.txt (it pulls a private litellm wheel and
# emergentintegrations, neither of which installs outside Emergent).

fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv==1.2.2

# Mongo
motor==3.3.1
pymongo==4.6.3

# Models / validation
pydantic==2.7.4
email-validator==2.3.0

# Auth
bcrypt==4.1.3
PyJWT==2.8.0
python-multipart==0.0.9

# Exports
reportlab==4.2.2
openpyxl==3.1.5
